/**
 * Hook for managing music playback during workouts.
 * This is a re-export of useMusic from MusicProvider for backward compatibility.
 */
export { useMusic as useMusicPlayer } from '../providers/MusicProvider';
